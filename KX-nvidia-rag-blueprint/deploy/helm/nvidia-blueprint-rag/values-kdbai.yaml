# SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# ---------------------------------------------------------------------------
# DEPRECATED — this file is now a backward-compat shim.
# ---------------------------------------------------------------------------
# Starting with this KDB.AI-only fork, all KDB.AI settings (image, kdbai block,
# envVars, ingestor-server config, GPU/cuVS toggles) are baked into values.yaml
# as defaults. You no longer need `-f values-kdbai.yaml` in your helm command.
#
# Old (still works):
#   helm install rag . -f values-kdbai.yaml -f deploy/EKS/rag-values-kdbai.yaml
#
# New (recommended):
#   helm install rag . -f deploy/EKS/rag-values-kdbai.yaml
#
# This file will be removed in a future release. Keep using it if your
# automation passes it; the contents below are a no-op since all settings
# match values.yaml defaults.

# (intentionally empty)
